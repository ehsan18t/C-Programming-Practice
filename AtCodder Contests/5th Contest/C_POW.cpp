#include <bits/stdc++.h>

#ifndef online_judge
#include "./debugging tools/dbg.cpp"
#include "./debugging tools/memview.cpp"
#endif

int main()
{
    // char ch;
    // long long a, b, c;
    // std::cin >> a >> b >> c;
    // if (a + b == 0 && c % 2 == 0)
    //     ch = '=';
    // else if (a > b)
    //     ch = '>';
    // else if (b > a)
    //     ch = '<';
    // else if (a == b)
    //     ch = '=';
    // std::cout << ch << std::endl;
    dbg(pow(3, -2));
    return 0;
}