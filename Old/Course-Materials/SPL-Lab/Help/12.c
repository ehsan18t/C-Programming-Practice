#include <stdio.h>

int main()
{
    int arr[100][100];
    int n, half;
    int result = 0;
    scanf("%d", &n);
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {
            scanf("%d", &arr[i][j]);
        }
    }
    for (int i = 0; i < n; i++)
    {
        
    }

    printf("%d", result);
    return 0;
}