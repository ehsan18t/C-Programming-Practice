#include <bits/stdc++.h>

using namespace std;

double running_med(int arr[], int n)
{
    int ele;
    cin >> ele;

    return ele;
}

int main()
{
    int n;
    queue<int> a;
    queue<int> b;
    cin >> n;

    while (n--)
    {
        cout << running_med(arr, n) << endl;
    }
    return 0;
}